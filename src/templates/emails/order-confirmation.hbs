<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; margin-bottom: 30px; }
    .content { margin-bottom: 30px; }
    .order-details { background: #f9f9f9; padding: 20px; margin: 20px 0; }
    .item { margin-bottom: 10px; }
    .total { font-weight: bold; margin-top: 20px; }
    .footer { text-align: center; font-size: 12px; color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Order Confirmation</h1>
      <h2>Order #{{orderNumber}}</h2>
    </div>
    
    <div class="content">
      <p>Dear {{customerName}},</p>
      
      <p>Thank you for your order! We're excited to fulfill it for you.</p>
      
      <div class="order-details">
        <h3>Order Details</h3>
        <p>Order Date: {{orderDate}}</p>
        
        <h4>Items:</h4>
        {{#each items}}
        <div class="item">
          <p>
            {{name}} x {{quantity}}
            {{#if cakeWriting}}<br>Cake Writing: {{cakeWriting}}{{/if}}
            <br>Price: {{totalPrice}}
          </p>
        </div>
        {{/each}}
        
        <div class="total">
          <p>Subtotal: {{subtotal}}</p>
          <p>Tax: {{tax}}</p>
          <p>Total: {{total}}</p>
        </div>

        {{#if deliveryDate}}
        <h4>Delivery Information:</h4>
        <p>Delivery Date: {{deliveryDate}}</p>
        <p>Delivery Time: {{deliveryTime}}</p>
        {{#if deliveryInstructions}}
        <p>Special Instructions: {{deliveryInstructions}}</p>
        {{/if}}
        <p>Delivery Address:<br>{{shippingAddress}}</p>
        {{/if}}

        {{#if isGift}}
        <h4>Gift Information:</h4>
        <p>Gift Message: {{giftMessage}}</p>
        <p>Recipient: {{giftRecipient.name}}</p>
        <p>Recipient Phone: {{giftRecipient.phone}}</p>
        {{/if}}
      </div>

      <p>Order Status: {{orderStatus}}</p>
      <p>Payment Status: {{paymentStatus}}</p>
      
      <p>We'll keep you updated on your order status. If you have any questions, please don't hesitate to contact us.</p>
    </div>

    <div class="footer">
      <p>© {{currentYear}} Pinewraps. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
